#include "main.h"

/**
 * mul - Multiplies two integers
 * @x: first number to be multiply
 * @y: second number to be multiplied with first
 * Return: result of multiplication between x and y
 */
int mul(int x, int y)
{
	return (x * y);
}
