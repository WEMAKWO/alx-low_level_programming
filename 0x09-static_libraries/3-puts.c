#include "main.h"
#include<stdio.h>
/**
 * _puts - a program to print a string
 * @str: string param
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
void _puts(char *str)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
